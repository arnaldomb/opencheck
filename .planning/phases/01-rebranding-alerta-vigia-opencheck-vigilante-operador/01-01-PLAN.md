---
phase: 01-rebranding-alerta-vigia-opencheck-vigilante-operador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/api/package.json
  - apps/web/package.json
  - packages/database/package.json
  - packages/asaas-sdk/package.json
  - packages/ctrlsafe-sdk/package.json
  - packages/ezviz-sdk/package.json
  - packages/shared/package.json
  - packages/ui/package.json
autonomous: true
requirements:
  - REQ-01

must_haves:
  truths:
    - "Nenhum arquivo package.json no monorepo contém o namespace @alerta-vigia"
    - "Todos os scripts db:* na raiz filtram por @opencheck/database"
    - "O campo name do package.json raiz é opencheck"
    - "Todos os imports workspace:* em apps/api/package.json e apps/web/package.json referenciam @opencheck/*"
  artifacts:
    - path: "package.json"
      provides: "Raiz do monorepo com name=opencheck e scripts db:* apontando para @opencheck/database"
      contains: "\"name\": \"opencheck\""
    - path: "apps/api/package.json"
      provides: "Package da API com nome @opencheck/api e dependências @opencheck/*"
      contains: "\"name\": \"@opencheck/api\""
    - path: "packages/database/package.json"
      provides: "Package do banco com nome @opencheck/database"
      contains: "\"name\": \"@opencheck/database\""
  key_links:
    - from: "package.json (raiz)"
      to: "scripts db:migrate"
      via: "pnpm --filter @opencheck/database"
      pattern: "@opencheck/database"
    - from: "apps/api/package.json"
      to: "packages/database"
      via: "workspace:* dependency"
      pattern: "@opencheck/database.*workspace"
---

<objective>
Renomear todos os nomes de pacotes npm do namespace `@alerta-vigia` para `@opencheck` em cada `package.json` do monorepo, incluindo o `package.json` raiz. Nenhum código TypeScript é tocado neste plano — apenas manifests de pacote.

Purpose: Estabelecer a identidade @opencheck como fundação antes de atualizar imports no código.
Output: 9 arquivos package.json com nomes e dependências atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Renomear packages nos manifests npm</name>
  <files>
    package.json,
    apps/api/package.json,
    apps/web/package.json,
    packages/database/package.json,
    packages/asaas-sdk/package.json,
    packages/ctrlsafe-sdk/package.json,
    packages/ezviz-sdk/package.json,
    packages/shared/package.json,
    packages/ui/package.json
  </files>
  <read_first>
    - package.json (raiz) — ver scripts db:* e name atual
    - apps/api/package.json — ver dependências @alerta-vigia/* atuais
    - apps/web/package.json — ver dependências @alerta-vigia/* atuais
    - packages/database/package.json — nome atual e scripts
  </read_first>
  <action>
Aplicar EXATAMENTE as seguintes substituições em cada arquivo:

**package.json (raiz):**
- `"name": "alerta-vigia"` → `"name": "opencheck"`
- Em cada script db:*: `--filter @alerta-vigia/database` → `--filter @opencheck/database`
  - `"db:generate": "pnpm --filter @opencheck/database prisma generate"`
  - `"db:migrate": "pnpm --filter @opencheck/database prisma migrate dev"`
  - `"db:push": "pnpm --filter @opencheck/database prisma db push"`
  - `"db:seed": "pnpm --filter @opencheck/database prisma db seed"`
  - `"db:studio": "pnpm --filter @opencheck/database prisma studio"`

**apps/api/package.json:**
- `"name": "@alerta-vigia/api"` → `"name": "@opencheck/api"`
- Em dependencies: substituir TODAS as ocorrências de `"@alerta-vigia/` por `"@opencheck/`:
  - `"@alerta-vigia/asaas-sdk"` → `"@opencheck/asaas-sdk"`
  - `"@alerta-vigia/ctrlsafe-sdk"` → `"@opencheck/ctrlsafe-sdk"`
  - `"@alerta-vigia/database"` → `"@opencheck/database"`
  - `"@alerta-vigia/ezviz-sdk"` → `"@opencheck/ezviz-sdk"`
  - `"@alerta-vigia/shared"` → `"@opencheck/shared"`

**apps/web/package.json:**
- `"name": "@alerta-vigia/web"` → `"name": "@opencheck/web"`
- Em dependencies: `"@alerta-vigia/shared"` → `"@opencheck/shared"`

**packages/database/package.json:**
- `"name": "@alerta-vigia/database"` → `"name": "@opencheck/database"`

**packages/asaas-sdk/package.json:**
- `"name": "@alerta-vigia/asaas-sdk"` → `"name": "@opencheck/asaas-sdk"`
- Se houver dependências `@alerta-vigia/*` em dependencies, renomear também.

**packages/ctrlsafe-sdk/package.json:**
- `"name": "@alerta-vigia/ctrlsafe-sdk"` → `"name": "@opencheck/ctrlsafe-sdk"`
- Se houver dependências `@alerta-vigia/*`, renomear também.

**packages/ezviz-sdk/package.json:**
- `"name": "@alerta-vigia/ezviz-sdk"` → `"name": "@opencheck/ezviz-sdk"`
- Se houver dependências `@alerta-vigia/*`, renomear também.

**packages/shared/package.json:**
- `"name": "@alerta-vigia/shared"` → `"name": "@opencheck/shared"`

**packages/ui/package.json:**
- `"name": "@alerta-vigia/ui"` → `"name": "@opencheck/ui"`
- Se houver dependências `@alerta-vigia/*`, renomear também.

NUNCA adicionar ou remover campos — apenas substituir valores de string existentes.
</action>
  <verify>
    <automated>grep -r "@alerta-vigia" package.json apps/api/package.json apps/web/package.json packages/database/package.json packages/asaas-sdk/package.json packages/ctrlsafe-sdk/package.json packages/ezviz-sdk/package.json packages/shared/package.json packages/ui/package.json 2>/dev/null && echo "FALHOU: ainda há @alerta-vigia" || echo "OK: nenhum @alerta-vigia nos package.json"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "@alerta-vigia" nos 9 arquivos package.json listados retorna saída vazia
    - package.json raiz contém "name": "opencheck"
    - package.json raiz contém "pnpm --filter @opencheck/database" em todos os scripts db:*
    - apps/api/package.json contém "name": "@opencheck/api"
    - packages/database/package.json contém "name": "@opencheck/database"
    - Nenhum valor de campo foi removido — apenas strings substituídas
  </acceptance_criteria>
  <done>Todos os 9 package.json têm nomes @opencheck/* e zero referências a @alerta-vigia</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| package.json → pnpm resolver | Nomes de pacotes são usados pelo resolver de workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json | accept | Operação de renomeação local; sem superfície de ataque nova |
</threat_model>

<verification>
Após execução:
1. `grep -r "@alerta-vigia" apps/*/package.json packages/*/package.json package.json` deve retornar vazio
2. `cat package.json | grep '"name"'` deve mostrar `"name": "opencheck"`
3. `cat apps/api/package.json | grep '"name"'` deve mostrar `"name": "@opencheck/api"`
</verification>

<success_criteria>
- Zero ocorrências de `@alerta-vigia` nos arquivos package.json do monorepo
- Todos os scripts db:* na raiz filtram por `@opencheck/database`
- pnpm consegue resolver o grafo de workspaces após a renomeação
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-01-SUMMARY.md`
</output>
