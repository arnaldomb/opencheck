---
phase: 01-rebranding-alerta-vigia-opencheck-vigilante-operador
plan: 05
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - REQ-01
  - REQ-02
  - REQ-03

must_haves:
  truths:
    - "pnpm typecheck executa sem erros TypeScript"
    - "pnpm build compila sem erros"
    - "Nenhuma ocorrência de @alerta-vigia em imports TypeScript"
    - "Nenhuma ocorrência de prisma.vigilante no código"
    - "Nenhuma ocorrência de @alerta-vigia em qualquer package.json do monorepo"
    - "pnpm-workspace.yaml e turbo.json não contêm referências a alerta-vigia"
  artifacts:
    - path: "apps/api/dist/"
      provides: "Build compilado da API sem erros"
    - path: "apps/web/.next/"
      provides: "Build compilado do frontend sem erros"
  key_links:
    - from: "pnpm typecheck"
      to: "zero erros TypeScript"
      via: "tsc --noEmit em todos os workspaces"
      pattern: "error TS"
---

<objective>
Executar verificações finais do rebranding: typecheck TypeScript em todo o monorepo, build de produção, e varreduras de grep para confirmar que zero referências antigas sobreviveram.

Purpose: Garantir que o rebranding é completo e o código compila antes de marcar a fase como concluída.
Output: Evidência de que typecheck e build passam; zero ocorrências de nomes antigos em código fonte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Varredura de completude e correção de remanescentes</name>
  <files></files>
  <read_first>
    - Nenhum arquivo específico — esta tarefa executa comandos de busca
  </read_first>
  <action>
Executar cada varredura abaixo. Para CADA ocorrência encontrada, corrigir no arquivo correspondente antes de prosseguir para a próxima verificação.

**Varredura 1 — imports @alerta-vigia:**
```bash
grep -r "@alerta-vigia" apps/ packages/ --include="*.ts" --include="*.tsx" --include="*.js" -l
```
Para cada arquivo listado: ler, substituir `@alerta-vigia/` por `@opencheck/` nos imports, salvar.
Resultado esperado: nenhum arquivo.

**Varredura 2 — uso de prisma.vigilante:**
```bash
grep -r "prisma\.vigilante" apps/ packages/ --include="*.ts" -l
```
Para cada arquivo listado: ler, substituir `prisma.vigilante.` por `prisma.operador.`, salvar.
Resultado esperado: nenhum arquivo.

**Varredura 3 — enum VIGILANTE em código (não schema):**
```bash
grep -r "'VIGILANTE'\|\"VIGILANTE\"" apps/ --include="*.ts" --include="*.tsx" -l
```
Para cada arquivo listado: avaliar contexto. Se for referência a AgentKeyTipo ou Papel, substituir por `'OPERADOR'`. Se for texto de mensagem de usuário, substituir por `'Operador'`.
Resultado esperado: nenhum arquivo.

**Varredura 4 — textos visíveis "Vigilante" no frontend:**
```bash
grep -r "Vigilante\|vigilante" apps/web/ --include="*.tsx" --include="*.ts" -l
```
Verificar cada arquivo. Se for interface TypeScript local ou texto visível, renomear para Operador/operador.
EXCEÇÃO PERMITIDA: nenhuma — o diretório `apps/web/app/(dashboard)/vigilantes/` deve ter sido deletado pelo plano 01-04. Se ainda existir, deletar agora.
Resultado esperado: nenhum arquivo.

**Varredura 5 — package.json com @alerta-vigia:**
```bash
grep -r "@alerta-vigia" --include="package.json" .
```
Resultado esperado: nenhum resultado (exceto pnpm-lock.yaml que é gerado automaticamente).

**Varredura 6 — docker-compose com alertavigia:**
```bash
grep -r "alertavigia" docker-compose*.yml
```
Resultado esperado: nenhum resultado.

**Varredura 7 — pnpm-workspace.yaml e turbo.json:**
```bash
grep -r "alerta.vigia\|alerta-vigia\|alertavigia" pnpm-workspace.yaml turbo.json 2>/dev/null
```
Para cada ocorrência encontrada:
- Em `pnpm-workspace.yaml`: substituir qualquer referência a `@alerta-vigia/*` ou pacotes com nome antigo por `@opencheck/*`
- Em `turbo.json`: substituir filtros ou dependências que referenciem `@alerta-vigia/` por `@opencheck/`
Resultado esperado: nenhum resultado.

Para cada problema encontrado: corrigir e re-executar a varredura correspondente.
</action>
  <verify>
    <automated>grep -r "@alerta-vigia" apps/ packages/ --include="*.ts" --include="*.tsx" 2>/dev/null | wc -l && grep -r "prisma\.vigilante" apps/ --include="*.ts" 2>/dev/null | wc -l && grep -r "alerta.vigia\|alerta-vigia" pnpm-workspace.yaml turbo.json 2>/dev/null | wc -l && echo "Contagens acima devem ser 0 0 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@alerta-vigia" apps/ packages/ --include="*.ts" --include="*.tsx"` retorna vazio
    - `grep -r "prisma.vigilante" apps/ packages/ --include="*.ts"` retorna vazio
    - `grep -r "@alerta-vigia" --include="package.json" apps/ packages/` retorna vazio
    - `grep "alertavigia" docker-compose.yml` retorna vazio
    - `grep -r "alerta.vigia\|alerta-vigia" pnpm-workspace.yaml turbo.json 2>/dev/null` retorna vazio
  </acceptance_criteria>
  <done>Todas as varreduras retornam vazio; eventuais remanescentes foram corrigidos</done>
</task>

<task type="auto">
  <name>Tarefa 2: pnpm typecheck e pnpm build</name>
  <files></files>
  <read_first>
    - Nenhum arquivo — esta tarefa executa comandos de build/typecheck
  </read_first>
  <action>
**Passo 1 — Typecheck:**
```bash
pnpm typecheck
```
Aguardar conclusão. Se houver erros TypeScript:
- Ler cada erro com atenção ao arquivo e linha
- Erros de tipo relacionados a `prisma.vigilante` → corrigir para `prisma.operador`
- Erros de tipo relacionados a `@alerta-vigia/` imports → corrigir para `@opencheck/`
- Erros de tipo relacionados a enum `VIGILANTE` → corrigir para `OPERADOR`
- Erros de tipo relacionados a campo `Tenant.vigilantes` → corrigir para `Tenant.operadores`
- Erros de tipo relacionados a campo `Ponto.vigilantes` → corrigir para `Ponto.operadores`
- Erros de tipo relacionados a `OnboardingStep.vigilante` → corrigir para `OnboardingStep.operador`
- Erros de tipo relacionados a `agentCtx.vigilanteId` → corrigir para `agentCtx.operadorId`
Re-executar typecheck após cada correção em lote.

**Passo 2 — Build (se typecheck passou):**
```bash
pnpm build
```
Se o build falhar por razões NÃO relacionadas ao rebranding (ex.: variáveis de ambiente ausentes em build de produção), documentar no SUMMARY.md mas não bloquear a fase.
Se falhar por razões DO rebranding, corrigir e re-executar.

**Passo 3 — Documentar resultado:**
Registrar no SUMMARY.md:
- Se `pnpm typecheck` passou: ✓ com zero erros
- Se `pnpm build` passou: ✓ sem erros
- Quaisquer correções aplicadas durante esta tarefa
</action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` termina com código de saída 0 (sem erros TypeScript)
    - Nenhum erro da forma "error TS2xxx" no output do typecheck
    - Se `pnpm build` executado: termina com código de saída 0
  </acceptance_criteria>
  <done>pnpm typecheck passa sem erros TypeScript; build compila com sucesso</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Rebranding completo do monorepo: pacotes renomeados para @opencheck/*, modelo Vigilante renomeado para Operador no schema e código, migration aplicada, módulo API vigilantes → operadores, rota /operadores, Field API /operador/:id/config, AgentContext.operadorId, frontend com sidebar "Operadores" e logo "OpenCheck", containers Docker opencheck_*, .env.example atualizado, pnpm-workspace.yaml e turbo.json sem refs antigas.
  </what-built>
  <how-to-verify>
1. Confirmar que o typecheck passou: `pnpm typecheck` (deve completar sem erros)
2. Verificar a sidebar do frontend: iniciar o app (`pnpm dev`) e acessar http://localhost:3000 — o menu deve mostrar "Operadores" (não "Vigilantes") e o logo "OpenCheck"
3. Verificar a rota: acessar http://localhost:3000/operadores — deve carregar a página (não 404)
4. Verificar zero refs antigas: `grep -r "@alerta-vigia" apps/ packages/ --include="*.ts" --include="*.tsx"` deve retornar vazio
5. Verificar schema: `grep "model Operador" packages/database/prisma/schema.prisma` deve retornar 1 resultado
6. Verificar .env.example: `grep "alertavigia" .env.example` deve retornar vazio
  </how-to-verify>
  <resume-signal>Digite "aprovado" quando todas as verificações passarem, ou descreva os problemas encontrados para correção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| CI/CD → pnpm build | Build artifact produzido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | build artifacts | accept | Build local de verificação; CI/CD aplica controles de pipeline |
</threat_model>

<verification>
Critérios finais da Fase 1:
1. `pnpm typecheck` → exit code 0
2. `grep -r "@alerta-vigia" apps/ packages/ --include="*.ts" --include="*.tsx"` → vazio
3. `grep -r "prisma\.vigilante" apps/` → vazio
4. `grep "model Operador" packages/database/prisma/schema.prisma` → 1 resultado
5. `grep "model Vigilante" packages/database/prisma/schema.prisma` → vazio
6. `grep "VIGILANTE" packages/database/prisma/schema.prisma` → vazio
7. `grep "opencheck_postgres" docker-compose.yml` → resultado
8. `grep "OpenCheck" apps/web/app/_components/SidebarLayout.tsx` → resultado
9. `grep "alertavigia" .env.example` → vazio
10. `grep -r "alerta.vigia\|alerta-vigia" pnpm-workspace.yaml turbo.json 2>/dev/null` → vazio
</verification>

<success_criteria>
- pnpm typecheck: ZERO erros TypeScript
- pnpm build: compila sem erros
- Zero refs @alerta-vigia em código TypeScript
- Zero refs prisma.vigilante em código TypeScript
- Schema Prisma com modelo Operador e enums atualizados
- Frontend exibe "OpenCheck" e "Operadores"
- Docker Compose com containers opencheck_*
- .env.example sem alertavigia.com.br ou /alertavigia
- pnpm-workspace.yaml e turbo.json sem referências a alerta-vigia
- Usuário aprova o checkpoint de verificação
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-05-SUMMARY.md`

Atualizar `.planning/STATE.md`:
- Phase: 1 of 7 → concluída
- Current focus: Phase 2 — Schema Módulo de Abertura
- Progress: 14% (1/7 fases concluídas)
</output>
