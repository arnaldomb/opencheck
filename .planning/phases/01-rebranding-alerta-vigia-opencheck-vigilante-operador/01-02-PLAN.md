---
phase: 01-rebranding-alerta-vigia-opencheck-vigilante-operador
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/database/prisma/schema.prisma
autonomous: true
requirements:
  - REQ-02

must_haves:
  truths:
    - "O modelo Prisma se chama Operador (não Vigilante)"
    - "A relação many-to-many usa o nome OperadorPontos (não VigilantePontos)"
    - "O campo Tenant.operadores existe (não Tenant.vigilantes)"
    - "O campo Ponto.operadores existe (não Ponto.vigilantes)"
    - "O enum Papel contém OPERADOR mas não contém VIGILANTE"
    - "O enum AgentKeyTipo contém OPERADOR mas não contém VIGILANTE"
    - "O campo OnboardingStep.operador existe (não OnboardingStep.vigilante)"
    - "A migration foi criada e aplicada com sucesso (BLOCKING)"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "Schema com modelo Operador, enums renomeados, relações atualizadas"
      contains: "model Operador"
    - path: "packages/database/prisma/migrations/"
      provides: "Migration rename_vigilante_to_operador aplicada"
      contains: "rename_vigilante_to_operador"
  key_links:
    - from: "model Operador"
      to: "model Tenant"
      via: "campo Tenant.operadores"
      pattern: "operadores.*Operador"
    - from: "model Operador"
      to: "model Ponto"
      via: "relação many-to-many OperadorPontos"
      pattern: "OperadorPontos"
    - from: "enum AgentKeyTipo"
      to: "model AgentKeyLog"
      via: "campo tipo: AgentKeyTipo"
      pattern: "OPERADOR"
---

<objective>
Renomear o modelo `Vigilante` para `Operador` no schema Prisma com todas as relações, campos e enums associados, e criar e executar a migration de banco de dados correspondente.

Esta é uma tarefa BLOCKING: o plano 01-03 depende dos tipos Prisma gerados aqui.

Purpose: Tornar o banco de dados e os tipos TypeScript gerados pelo Prisma coerentes com a nova terminologia.
Output: schema.prisma atualizado + migration criada + `prisma generate` atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-CONTEXT.md
@packages/database/prisma/schema.prisma

<interfaces>
<!-- Estado atual do schema — leitura obrigatória antes de editar -->
<!-- Modelo a renomear: -->
model Vigilante {
  id         String   @id @default(cuid())
  tenantId   String
  nome       String
  telefone   String?
  rfid       String?
  codigo     String?
  ativo      Boolean  @default(true)
  criadoEm  DateTime @default(now())
  agentKey   String?  @unique
  agentKeyAt DateTime?
  tenant Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  pontos Ponto[] @relation("VigilantePontos")
  @@unique([tenantId, codigo])
}

<!-- Referências ao modelo em Tenant e Ponto: -->
model Tenant {
  ...
  vigilantes   Vigilante[]   -- RENOMEAR para: operadores   Operador[]
}

model Ponto {
  ...
  vigilantes  Vigilante[]    @relation("VigilantePontos")  -- RENOMEAR para: operadores  Operador[] @relation("OperadorPontos")
}

<!-- OnboardingStep: -->
model OnboardingStep {
  ...
  vigilante   Boolean   @default(false)  -- RENOMEAR para: operador   Boolean   @default(false)
}

<!-- Enums: -->
enum Papel {
  SUPERADMIN
  ADMIN
  OPERADOR
  VIGILANTE    -- REMOVER este valor
}

enum AgentKeyTipo {
  PONTO
  VIGILANTE    -- RENOMEAR para: OPERADOR
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Atualizar schema.prisma — renomear modelo, relações e enums</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    - packages/database/prisma/schema.prisma — ler inteiro antes de qualquer edição
  </read_first>
  <action>
Aplicar as seguintes modificações EXATAS no arquivo `packages/database/prisma/schema.prisma`:

**1. Renomear o modelo Vigilante → Operador:**
Localizar o bloco que começa com `model Vigilante {` e:
- Renomear para `model Operador {`
- Na linha com a relação pontos, trocar `@relation("VigilantePontos")` por `@relation("OperadorPontos")`
- Manter todos os campos inalterados (id, tenantId, nome, telefone, rfid, codigo, ativo, criadoEm, agentKey, agentKeyAt, tenant, pontos, @@unique)

Resultado esperado:
```prisma
model Operador {
  id         String   @id @default(cuid())
  tenantId   String
  nome       String
  telefone   String?
  rfid       String?
  codigo     String?
  ativo      Boolean  @default(true)
  criadoEm  DateTime @default(now())

  agentKey   String?  @unique
  agentKeyAt DateTime?

  tenant Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  pontos Ponto[] @relation("OperadorPontos")

  @@unique([tenantId, codigo])
}
```

**2. Atualizar model Tenant — campo vigilantes → operadores:**
Localizar dentro de `model Tenant`:
- `vigilantes   Vigilante[]` → `operadores   Operador[]`

**3. Atualizar model Ponto — campo vigilantes → operadores:**
Localizar dentro de `model Ponto`:
- `vigilantes  Vigilante[]    @relation("VigilantePontos")` → `operadores  Operador[]  @relation("OperadorPontos")`

**4. Atualizar model OnboardingStep — campo vigilante → operador:**
Localizar dentro de `model OnboardingStep`:
- `vigilante   Boolean   @default(false)` → `operador   Boolean   @default(false)`

**5. Atualizar enum Papel — remover VIGILANTE:**
Localizar `enum Papel {` e remover a linha `VIGILANTE`. O enum deve ficar:
```prisma
enum Papel {
  SUPERADMIN
  ADMIN
  OPERADOR
}
```
ATENÇÃO: verificar se `Usuario.papel` usa `@default(OPERADOR)` — OPERADOR já existe no enum, não alterar esse default.

**6. Atualizar enum AgentKeyTipo — VIGILANTE → OPERADOR:**
Localizar `enum AgentKeyTipo {` e:
- Renomear `VIGILANTE` para `OPERADOR`
Resultado:
```prisma
enum AgentKeyTipo {
  PONTO
  OPERADOR
}
```

Não alterar nenhum outro campo, modelo ou enum além dos listados acima.
</action>
  <verify>
    <automated>grep -n "Vigilante\|VigilantePontos\|AgentKeyTipo.*VIGILANTE" packages/database/prisma/schema.prisma && echo "FALHOU: ainda há referências Vigilante" || echo "OK: schema sem Vigilante"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "model Vigilante" packages/database/prisma/schema.prisma` retorna vazio
    - `grep "model Operador" packages/database/prisma/schema.prisma` retorna 1 resultado
    - `grep "VigilantePontos" packages/database/prisma/schema.prisma` retorna vazio
    - `grep "OperadorPontos" packages/database/prisma/schema.prisma` retorna 2 resultados (em Operador e Ponto)
    - `grep "vigilantes" packages/database/prisma/schema.prisma` retorna vazio
    - `grep "operadores" packages/database/prisma/schema.prisma` retorna 2 resultados (em Tenant e Ponto)
    - `grep "vigilante.*Boolean" packages/database/prisma/schema.prisma` retorna vazio (campo OnboardingStep renomeado)
    - `grep "operador.*Boolean" packages/database/prisma/schema.prisma` retorna 1 resultado
    - enum Papel não contém VIGILANTE
    - enum AgentKeyTipo contém OPERADOR (não VIGILANTE)
  </acceptance_criteria>
  <done>schema.prisma atualizado com modelo Operador, relações OperadorPontos, campo operadores, enum Papel sem VIGILANTE, AgentKeyTipo com OPERADOR</done>
</task>

<task type="auto">
  <name>Tarefa 2: [BLOCKING] Executar migration Prisma e regenerar cliente</name>
  <files>packages/database/prisma/migrations/</files>
  <read_first>
    - packages/database/prisma/schema.prisma — confirmar que Tarefa 1 foi concluída (grep "model Operador")
  </read_first>
  <action>
Executar em sequência:

**Passo 1 — Criar e aplicar a migration:**
```bash
pnpm --filter @opencheck/database db:migrate -- --name rename_vigilante_to_operador
```

Se o comando falhar por ser ambiente non-TTY, usar:
```bash
pnpm --filter @opencheck/database exec prisma migrate dev --name rename_vigilante_to_operador
```

Aguardar conclusão. A migration renomeia a tabela `Vigilante` para `Operador`, renomeia a tabela intermediária `_VigilantePontos` para `_OperadorPontos`, renomeia colunas conforme definido pelo Prisma, e remove o valor de enum VIGILANTE de `Papel` e `AgentKeyTipo`.

ATENÇÃO: Se o banco de dados não estiver acessível (ambiente de desenvolvimento sem Docker rodando), documentar esse fato no SUMMARY.md e marcar como "a executar quando o banco estiver disponível". NÃO falhar o plano por isso — o schema está correto.

**Passo 2 — Regenerar o cliente Prisma:**
```bash
pnpm --filter @opencheck/database exec prisma generate
```

Isso atualiza os tipos TypeScript em `node_modules/@prisma/client` para refletir `prisma.operador` em vez de `prisma.vigilante`.

**Passo 3 — Verificar geração:**
```bash
grep -r "operador\b" node_modules/.pnpm/@prisma+client*/node_modules/@prisma/client/index.d.ts 2>/dev/null | head -5
```
Se o grep retornar resultados, a geração foi bem-sucedida.
</action>
  <verify>
    <automated>ls packages/database/prisma/migrations/ | grep rename_vigilante_to_operador && echo "OK: migration criada" || echo "AVISO: migration não encontrada (banco pode estar offline)"</automated>
  </verify>
  <acceptance_criteria>
    - Diretório `packages/database/prisma/migrations/` contém subdiretório com sufixo `rename_vigilante_to_operador`
    - O arquivo `migration.sql` dentro dessa pasta existe
    - `pnpm --filter @opencheck/database exec prisma generate` executa sem erros
    - Após generate, `prisma.vigilante` não existe mais no cliente (apenas `prisma.operador`)
  </acceptance_criteria>
  <done>Migration aplicada com sucesso e cliente Prisma regenerado com tipos Operador</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| schema.prisma → banco PostgreSQL | Migrations alteram estrutura de tabelas em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | migration SQL | mitigate | Usar `prisma migrate dev` (não `db push`) para preservar histórico de migrations e permitir rollback |
| T-01-02-02 | Denial of Service | migration em produção | accept | Fase 1 é rebranding em ambiente de desenvolvimento; produção requer deploy controlado |
</threat_model>

<verification>
1. `grep "model Operador" packages/database/prisma/schema.prisma` → 1 resultado
2. `grep "model Vigilante" packages/database/prisma/schema.prisma` → 0 resultados
3. `grep "VIGILANTE" packages/database/prisma/schema.prisma` → 0 resultados
4. `ls packages/database/prisma/migrations/ | grep rename_vigilante` → pasta da migration existe
</verification>

<success_criteria>
- schema.prisma contém modelo Operador com relação OperadorPontos
- enum Papel não contém VIGILANTE
- enum AgentKeyTipo tem OPERADOR (não VIGILANTE)
- OnboardingStep tem campo operador (não vigilante)
- Migration `rename_vigilante_to_operador` criada e aplicada
- Cliente Prisma regenerado com `prisma.operador`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-02-SUMMARY.md`
</output>
