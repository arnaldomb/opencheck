---
phase: 01-rebranding-alerta-vigia-opencheck-vigilante-operador
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-03
files_modified:
  - apps/web/app/(dashboard)/vigilantes/page.tsx
  - apps/web/app/_components/SidebarLayout.tsx
  - apps/web/app/(dashboard)/onboarding/page.tsx
  - apps/web/lib/api.ts
  - docker-compose.yml
  - docker-compose.prod.yml
  - docker-compose.vps.yml
  - CLAUDE.md
  - Makefile
  - .env.example
autonomous: true
requirements:
  - REQ-02
  - REQ-03

must_haves:
  truths:
    - "A sidebar mostra 'Operadores' (não 'Vigilantes') no menu de navegação"
    - "A rota de navegação é /operadores (não /vigilantes)"
    - "O diretório apps/web/app/(dashboard)/operadores/ existe e a rota /operadores não resulta em 404"
    - "A página de operadores chama /operadores (não /vigilantes) na API"
    - "O onboarding mostra 'Cadastrar operador' e navega para /operadores"
    - "O campo OnboardingStep verifica 'operador' (não 'vigilante')"
    - "Os containers Docker têm nome opencheck_* (não alertavigia_*)"
    - "O banco PostgreSQL se chama opencheck (não alertavigia)"
    - "O CLAUDE.md reflete a identidade OpenCheck com terminologia Operador"
    - "Nenhum import @alerta-vigia no frontend web"
    - ".env.example não contém referências a alertavigia.com.br nem /alertavigia"
  artifacts:
    - path: "apps/web/app/(dashboard)/operadores/page.tsx"
      provides: "Página de operadores no diretório com rota correta do Next.js"
      contains: "Operadores"
    - path: "apps/web/app/_components/SidebarLayout.tsx"
      provides: "Sidebar com item Operadores apontando para /operadores"
      contains: "/operadores"
    - path: "docker-compose.yml"
      provides: "Containers renomeados para opencheck_*"
      contains: "opencheck_postgres"
    - path: ".env.example"
      provides: "Variáveis de ambiente sem referências a alertavigia.com.br"
      contains: "opencheck"
    - path: "CLAUDE.md"
      provides: "Documentação atualizada com identidade OpenCheck"
      contains: "OpenCheck"
  key_links:
    - from: "SidebarLayout.tsx NAV_USER"
      to: "/operadores"
      via: "href: '/operadores'"
      pattern: "href.*operadores"
    - from: "apps/web/app/(dashboard)/operadores/page.tsx"
      to: "apiFetch('/operadores')"
      via: "useEffect + apiFetch"
      pattern: "apiFetch.*operadores"
---

<objective>
Atualizar o frontend Next.js (página de vigilantes → operadores, sidebar, onboarding), os arquivos Docker Compose (containers e banco renomeados para opencheck), o `.env.example` (variáveis com alertavigia.com.br), e o CLAUDE.md com a nova identidade do produto.

Purpose: Completar o rebranding visual e de infraestrutura — nenhum usuário ou deploy deve ver referências a "Alerta Vigia" ou "Vigilante".
Output: Frontend com terminologia Operador, Docker com nomes opencheck_*, .env.example sem alertavigia.com.br, CLAUDE.md atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-CONTEXT.md
@apps/web/app/(dashboard)/vigilantes/page.tsx
@apps/web/app/_components/SidebarLayout.tsx
@apps/web/app/(dashboard)/onboarding/page.tsx

<interfaces>
<!-- API endpoints após plano 01-03: -->
<!-- GET /operadores     — lista operadores do tenant -->
<!-- POST /operadores    — cria operador -->
<!-- GET /operadores/:id -->
<!-- PUT /operadores/:id -->
<!-- DELETE /operadores/:id -->
<!-- POST /operadores/:id/codigo/gerar -->
<!-- POST /operadores/:id/agentkey/regenerar -->

<!-- Campo retornado pela API continua tendo vigilanteId: string (compatível com cliente externo) -->
<!-- Interface TypeScript frontend deve ser renomeada de Vigilante para Operador -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Atualizar frontend — página vigilantes → operadores (mover diretório), sidebar, onboarding</name>
  <files>
    apps/web/app/(dashboard)/operadores/page.tsx,
    apps/web/app/_components/SidebarLayout.tsx,
    apps/web/app/(dashboard)/onboarding/page.tsx,
    apps/web/lib/api.ts
  </files>
  <read_first>
    - apps/web/app/(dashboard)/vigilantes/page.tsx — conteúdo completo
    - apps/web/app/_components/SidebarLayout.tsx — ver NAV_USER e logo "Alerta Vigia"
    - apps/web/app/(dashboard)/onboarding/page.tsx — ver PASSOS e texto "Alerta Vigia"
    - apps/web/lib/api.ts — verificar se há import @alerta-vigia
  </read_first>
  <action>
**Renomear diretório de rota Next.js — OBRIGATÓRIO antes de qualquer edição de conteúdo:**

A SidebarLayout.tsx aponta `href: '/operadores'`. No Next.js App Router, a rota é determinada pelo diretório. Portanto:

1. Criar o diretório `apps/web/app/(dashboard)/operadores/`
2. Criar `apps/web/app/(dashboard)/operadores/page.tsx` com o conteúdo atualizado de `apps/web/app/(dashboard)/vigilantes/page.tsx` (ver substituições abaixo)
3. Deletar `apps/web/app/(dashboard)/vigilantes/page.tsx` e o diretório `apps/web/app/(dashboard)/vigilantes/`

```bash
mkdir -p apps/web/app/\(dashboard\)/operadores
# Copiar e transformar — ver substituições abaixo
rm -rf apps/web/app/\(dashboard\)/vigilantes/
```

**Conteúdo de apps/web/app/(dashboard)/operadores/page.tsx:**

Tomar o conteúdo de `apps/web/app/(dashboard)/vigilantes/page.tsx` e aplicar TODAS as seguintes substituições:

1. `interface Vigilante {` → `interface Operador {`
2. `const [vigilantes, setVigilantes] = useState<Vigilante[]>([])` → `const [operadores, setOperadores] = useState<Operador[]>([])`
3. `const vigs = await apiFetch<Vigilante[]>('/vigilantes')` → `const ops = await apiFetch<Operador[]>('/operadores')`
4. `setVigilantes(vigs)` → `setOperadores(ops)`
5. `return vigs.map(...)` (se presente) → `return ops.map(...)`
6. Em todo o JSX, `vigilantes.length` → `operadores.length`, `vigilantes.map(v =>` → `operadores.map(v =>`
7. `await apiFetch('/vigilantes', { method: 'POST', ...` → `await apiFetch('/operadores', { method: 'POST', ...`
8. `await apiFetch(\`/vigilantes/${editando.id}\`, { method: 'PUT', ...` → `await apiFetch(\`/operadores/${editando.id}\`, { method: 'PUT', ...`
9. `await apiFetch(\`/vigilantes/${v.id}\`, { method: 'DELETE' })` → `await apiFetch(\`/operadores/${v.id}\`, { method: 'DELETE' })`
10. `await apiFetch<{ id: string; codigo: string }>(\`/vigilantes/${v.id}/codigo/gerar\`` → `await apiFetch<{ id: string; codigo: string }>(\`/operadores/${v.id}/codigo/gerar\``
11. `if (!confirm(\`Desativar vigilante "${v.nome}"?\`))` → `if (!confirm(\`Desativar operador "${v.nome}"?\`))`
12. Textos visíveis ao usuário:
    - `'Vigilantes'` no h1 → `'Operadores'`
    - `{vigilantes.length} vigilante(s) cadastrado(s)` → `{operadores.length} operador(es) cadastrado(s)`
    - `'Nenhum vigilante cadastrado'` → `'Nenhum operador cadastrado'`
    - `'Cadastrar primeiro vigilante'` → `'Cadastrar primeiro operador'`
    - `'Novo vigilante'` nos botões e modal → `'Novo operador'`
    - No modal título: `modal === 'novo' ? 'Novo vigilante'` → `modal === 'novo' ? 'Novo operador'`
    - `\`Editar: ${editando?.nome}\`` — manter (não tem "vigilante")
    - `'Cadastrar'` button — manter
    - Label de campo `vigilanteId:` no card → `operadorId:` (o texto da label, não o campo da API)
    - `'Novo vigilante'` no button do header → `'Novo operador'`
13. Estado local: `const [editando, setEditando] = useState<Vigilante | null>(null)` → `useState<Operador | null>(null)`
14. `setVigilantes(prev => prev.map(...))` → `setOperadores(prev => prev.map(...))`
15. Qualquer outro texto "vigilante" em minúsculo visível ao usuário → "operador"
16. A `interface Vigilante` no estado `setEditando` e nas funções → usar `Operador`

IMPORTANTE: o campo `v.codigo` e a key `vigilanteId` no retorno da API são MANTIDOS (a API retorna `vigilanteId` por compatibilidade). O label `vigilanteId:` no card é a única mudança de label — renomear para `operadorId:`.

**apps/web/app/_components/SidebarLayout.tsx:**

1. Na constante NAV_USER:
   - `{ href: '/vigilantes', label: 'Vigilantes', icon: Users }` → `{ href: '/operadores', label: 'Operadores', icon: Users }`

2. No logo da sidebar:
   - `<span className="...">Alerta Vigia</span>` → `<span className="...">OpenCheck</span>`

3. Nenhuma outra mudança necessária.

**apps/web/app/(dashboard)/onboarding/page.tsx:**

1. Interface interna:
   - `interface OnboardingStep { ponto: boolean; vigilante: boolean; ciclo: boolean; notificacao: boolean; teste: boolean }` → `interface OnboardingStep { ponto: boolean; operador: boolean; ciclo: boolean; notificacao: boolean; teste: boolean }`

2. Constante PASSOS:
   - `{ key: 'vigilante', label: 'Cadastrar vigilante', href: '/vigilantes', desc: 'Adicione o vigilante responsável pelo ponto' }` → `{ key: 'operador', label: 'Cadastrar operador', href: '/operadores', desc: 'Adicione o operador responsável pelo ponto' }`

3. Verificação de conclusão:
   - `if (data?.ponto && data?.vigilante && data?.ciclo && ...` → `if (data?.ponto && data?.operador && data?.ciclo && ...`

4. `const concluidos = PASSOS.filter(p => steps[p.key]).length` — funciona automaticamente com a mudança de key.

5. Texto no h1:
   - `<h1>Bem-vindo ao Alerta Vigia</h1>` → `<h1>Bem-vindo ao OpenCheck</h1>`

**apps/web/lib/api.ts:**
- Ler o arquivo. Se houver `import ... from '@alerta-vigia/...'`, substituir por `@opencheck/...`.
- Se não houver imports @alerta-vigia, nenhuma mudança necessária.
</action>
  <verify>
    <automated>grep -r "Vigilante\|vigilantes\|Alerta Vigia\|@alerta-vigia" apps/web/app/_components/SidebarLayout.tsx apps/web/app/\(dashboard\)/onboarding/page.tsx apps/web/lib/api.ts 2>/dev/null && echo "ATENÇÃO: ainda há refs antigas" || echo "OK: componentes principais atualizados"</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/web/app/(dashboard)/operadores/page.tsx` existe
    - `ls apps/web/app/(dashboard)/vigilantes/ 2>/dev/null` retorna vazio (diretório deletado)
    - `grep "href.*vigilantes" apps/web/app/_components/SidebarLayout.tsx` retorna vazio
    - `grep "href.*operadores" apps/web/app/_components/SidebarLayout.tsx` retorna resultado
    - `grep "Alerta Vigia" apps/web/app/_components/SidebarLayout.tsx` retorna vazio
    - `grep "OpenCheck" apps/web/app/_components/SidebarLayout.tsx` retorna resultado
    - `grep "key: 'vigilante'" apps/web/app/(dashboard)/onboarding/page.tsx` retorna vazio
    - `grep "key: 'operador'" apps/web/app/(dashboard)/onboarding/page.tsx` retorna resultado
    - `grep "apiFetch.*'/operadores'" apps/web/app/(dashboard)/operadores/page.tsx` retorna resultado
    - `grep "@alerta-vigia" apps/web/lib/api.ts 2>/dev/null` retorna vazio
  </acceptance_criteria>
  <done>Frontend atualizado: diretório operadores/ criado, vigilantes/ deletado, sidebar com /operadores e "OpenCheck", onboarding com passo operador</done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar Docker Compose, .env.example, CLAUDE.md e Makefile</name>
  <files>
    docker-compose.yml,
    docker-compose.prod.yml,
    docker-compose.vps.yml,
    .env.example,
    CLAUDE.md,
    Makefile
  </files>
  <read_first>
    - docker-compose.yml — ver nomes de containers e banco
    - docker-compose.prod.yml — ver nomes de containers
    - .env.example — ver variáveis com alertavigia.com.br, /alertavigia na DATABASE_URL
    - CLAUDE.md — ver seções a atualizar
  </read_first>
  <action>
**docker-compose.yml — substituições:**
1. `container_name: alertavigia_postgres` → `container_name: opencheck_postgres`
2. `POSTGRES_DB: alertavigia` → `POSTGRES_DB: opencheck`
3. `DATABASE_URL: postgresql://postgres:postgres@postgres:5432/alertavigia` → `DATABASE_URL: postgresql://postgres:postgres@postgres:5432/opencheck`
4. `container_name: alertavigia_redis` → `container_name: opencheck_redis`
5. `container_name: alertavigia_migrate` → `container_name: opencheck_migrate`
6. `SUPERADMIN_EMAIL: admin@alertavigia.com.br` → `SUPERADMIN_EMAIL: admin@opencheck.com.br`
7. `container_name: alertavigia_api` → `container_name: opencheck_api`
8. `DATABASE_URL: postgresql://postgres:postgres@postgres:5432/alertavigia` (no serviço api) → `DATABASE_URL: postgresql://postgres:postgres@postgres:5432/opencheck`
9. `container_name: alertavigia_web` → `container_name: opencheck_web`
10. `container_name: alertavigia_pgadmin` → `container_name: opencheck_pgadmin`
11. `PGADMIN_DEFAULT_EMAIL: admin@alertavigia.com.br` → `PGADMIN_DEFAULT_EMAIL: admin@opencheck.com.br`
12. `container_name: alertavigia_minio` → `container_name: opencheck_minio`
13. `container_name: alertavigia_redis_commander` → `container_name: opencheck_redis_commander`
NÃO alterar: volumes, ports, imagens, lógica de healthcheck.

**docker-compose.prod.yml — substituições:**
Ler o arquivo completo. Substituir TODAS as ocorrências de:
- `alertavigia_` → `opencheck_` (em container_name)
- `alertavigia` como valor de POSTGRES_DB → `opencheck`
- `/alertavigia` em DATABASE_URL path → `/opencheck`
- `alertavigia.com.br` em emails de admin → `opencheck.com.br`
NÃO alterar: labels Traefik, volumes, ports, DOMAIN, imagens.

**docker-compose.vps.yml — substituições:**
Ler o arquivo completo. Aplicar as mesmas substituições de alertavigia → opencheck conforme acima.
Se o arquivo não existir, pular.

**.env.example — substituições obrigatórias:**
Ler o arquivo inteiro. Aplicar as seguintes substituições:
1. Na variável DATABASE_URL: `/alertavigia` → `/opencheck`
   - Exemplo: `DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alertavigia"` → `DATABASE_URL="postgresql://postgres:postgres@localhost:5432/opencheck"`
2. `EMAIL_FROM=.*@alertavigia.com.br` → `EMAIL_FROM=noreply@opencheck.com.br` (ou equivalente)
3. `SUPERADMIN_EMAIL=.*@alertavigia.com.br` → `SUPERADMIN_EMAIL=admin@opencheck.com.br`
4. `DOMAIN=alertavigia.com.br` → `DOMAIN=opencheck.com.br`
5. `ACME_EMAIL=.*@alertavigia.com.br` → `ACME_EMAIL=admin@opencheck.com.br`
6. Qualquer outra variável com valor contendo `alertavigia` → substituir por `opencheck`
NÃO alterar: nomes das variáveis de ambiente (ex.: EMAIL_FROM permanece EMAIL_FROM), comentários técnicos, valores de exemplo genéricos (ex.: "your_secret_here").

**CLAUDE.md — atualizar terminologia:**
Ler o arquivo inteiro. Aplicar:
1. Na seção "Visão Geral": "vigilância patrimonial" → pode manter (é domínio de negócio)
2. Na linha "Alarmes": remover "Evolution API" se não for relevante para OpenCheck, ou manter
3. Na seção "Estrutura do Monorepo":
   - `@alerta-vigia/api` → `@opencheck/api`
   - `@alerta-vigia/database` → `@opencheck/database`
   - Qualquer outra ref `@alerta-vigia/*` → `@opencheck/*`
4. Na seção "Comandos Essenciais":
   - `pnpm --filter @alerta-vigia/api dev` → `pnpm --filter @opencheck/api dev`
   - `pnpm --filter @alerta-vigia/web dev` → `pnpm --filter @opencheck/web dev`
5. Na seção "Convenções de Código":
   - Mencionar que a terminologia usa "Operador" (não "Vigilante")
6. Substituir "Alerta Vigia" por "OpenCheck" nos títulos e descrições que usam o nome do produto
7. NÃO alterar: regras técnicas, arquitetura, padrões de código — apenas nomes do produto e terminologia

**Makefile:**
Ler o arquivo. Não há referências a alertavigia nos targets — o Makefile usa `pnpm` e `docker compose` sem nomes de containers hardcoded. Verificar e, se houver alguma ref a alertavigia, substituir por opencheck. Se não houver, nenhuma mudança necessária.
</action>
  <verify>
    <automated>grep -r "alertavigia\|alerta-vigia\|Alerta Vigia" docker-compose.yml docker-compose.prod.yml CLAUDE.md .env.example 2>/dev/null && echo "ATENÇÃO: ainda há refs alertavigia" || echo "OK: infraestrutura e CLAUDE.md atualizados"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "alertavigia" docker-compose.yml` retorna vazio
    - `grep "opencheck_postgres" docker-compose.yml` retorna resultado
    - `grep "POSTGRES_DB.*opencheck" docker-compose.yml` retorna resultado
    - `grep "alertavigia" docker-compose.prod.yml` retorna vazio
    - `grep "alertavigia" .env.example` retorna vazio
    - `grep "/opencheck" .env.example` retorna resultado (DATABASE_URL atualizada)
    - `grep "@alerta-vigia" CLAUDE.md` retorna vazio
    - `grep "@opencheck" CLAUDE.md` retorna resultados (nas seções de estrutura e comandos)
  </acceptance_criteria>
  <done>Docker Compose com nomes opencheck_*, .env.example sem alertavigia.com.br, CLAUDE.md sem @alerta-vigia e com terminologia Operador/OpenCheck</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| docker-compose.yml → PostgreSQL | Nome do banco é configuração de infraestrutura |
| browser → /operadores | Rota frontend que chama API |
| .env.example → deploy | Arquivo de referência para configuração de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | docker-compose.yml POSTGRES_DB | mitigate | Renomear banco só em ambiente dev; produção requer backup antes do rename |
| T-01-04-02 | Spoofing | SidebarLayout NAV_USER | accept | Rota /operadores requer JWT válido via authMiddleware |
| T-01-04-03 | Information Disclosure | .env.example | accept | Arquivo de exemplo sem secrets reais; apenas valores de referência |
</threat_model>

<verification>
1. `grep "alertavigia" docker-compose.yml docker-compose.prod.yml` → vazio
2. `grep "container_name.*opencheck" docker-compose.yml` → múltiplos resultados
3. `grep "alertavigia" .env.example` → vazio
4. `grep "Alerta Vigia" apps/web/app/_components/SidebarLayout.tsx` → vazio
5. `grep "OpenCheck" apps/web/app/_components/SidebarLayout.tsx` → 1 resultado
6. `grep "/operadores" apps/web/app/_components/SidebarLayout.tsx` → 1 resultado
7. `ls apps/web/app/(dashboard)/operadores/page.tsx` → arquivo existe
8. `ls apps/web/app/(dashboard)/vigilantes/ 2>/dev/null` → vazio
</verification>

<success_criteria>
- docker-compose.yml: zero ocorrências de alertavigia, containers com opencheck_*
- .env.example: zero ocorrências de alertavigia.com.br ou /alertavigia
- CLAUDE.md: zero @alerta-vigia, usa @opencheck/* em exemplos de comandos
- Sidebar mostra "OpenCheck" no logo e "Operadores" como item de menu
- Diretório apps/web/app/(dashboard)/operadores/ existe (rota /operadores funciona no Next.js)
- Diretório apps/web/app/(dashboard)/vigilantes/ deletado
- Onboarding verifica passo `operador` (não `vigilante`)
- Nenhum import @alerta-vigia no frontend web
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-04-SUMMARY.md`
</output>
