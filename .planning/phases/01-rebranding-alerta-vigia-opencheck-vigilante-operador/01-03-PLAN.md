---
phase: 01-rebranding-alerta-vigia-opencheck-vigilante-operador
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/api/src/index.ts
  - apps/api/src/modules/vigilantes/vigilantes.routes.ts
  - apps/api/src/modules/field-api/field-api.middleware.ts
  - apps/api/src/modules/field-api/field-api.routes.ts
  - apps/api/src/modules/field-api/field-api.service.ts
  - apps/api/src/modules/pontos/pontos.routes.ts
  - apps/api/src/modules/configuracoes/configuracoes.routes.ts
  - apps/api/src/modules/eventos/eventos.routes.ts
  - apps/api/src/modules/notificacoes/notificacoes.routes.ts
  - apps/api/src/modules/auth/auth.routes.ts
  - apps/api/src/modules/cameras/cameras.routes.ts
  - apps/api/src/modules/ciclos/ciclos.routes.ts
  - apps/api/src/modules/relatorios/relatorios.routes.ts
  - apps/api/src/modules/superadmin/superadmin.routes.ts
  - apps/api/src/modules/webhooks/webhook.routes.ts
  - apps/api/src/modules/assinaturas/assinaturas.routes.ts
  - apps/api/src/modules/assinaturas/webhook.handler.ts
  - apps/api/src/modules/assinaturas/assinatura.service.ts
  - apps/api/src/middleware/assinatura.middleware.ts
  - apps/api/src/jobs/ciclo-alerta.job.ts
  - apps/api/src/jobs/ciclo-agendamento.job.ts
  - apps/api/src/jobs/notificacao.job.ts
  - apps/api/src/jobs/assinatura-sync.job.ts
  - apps/api/src/jobs/index.ts
  - apps/api/src/infra/asaas/asaas.client.ts
  - apps/api/src/infra/ezviz/ezviz.factory.ts
  - apps/api/src/infra/storage/storage.service.ts
autonomous: true
requirements:
  - REQ-01
  - REQ-02

must_haves:
  truths:
    - "Nenhum arquivo .ts na API importa de @alerta-vigia/*"
    - "O módulo vigilantes foi renomeado para operadores (pasta e arquivo)"
    - "A rota /vigilantes está registrada como /operadores em apps/api/src/index.ts"
    - "A função vigilantesRoutes foi renomeada para operadoresRoutes"
    - "prisma.vigilante.* foi substituído por prisma.operador.* em todos os arquivos da API"
    - "AgentContext.tipo usa 'OPERADOR' (não 'VIGILANTE')"
    - "A rota Field API /vigilante/:id/config foi renomeada para /operador/:id/config"
    - "Body field vigilanteId foi mantido mas referências internas apontam para operador"
  artifacts:
    - path: "apps/api/src/modules/operadores/operadores.routes.ts"
      provides: "Módulo de operadores renomeado de vigilantes"
      contains: "operadoresRoutes"
    - path: "apps/api/src/modules/field-api/field-api.middleware.ts"
      provides: "Middleware com tipo OPERADOR e prisma.operador"
      contains: "prisma.operador"
    - path: "apps/api/src/index.ts"
      provides: "Registro de rota /operadores"
      contains: "/operadores"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "apps/api/src/modules/operadores/operadores.routes.ts"
      via: "import operadoresRoutes + register /operadores"
      pattern: "operadoresRoutes"
    - from: "apps/api/src/modules/field-api/field-api.middleware.ts"
      to: "prisma.operador"
      via: "findUnique por agentKey"
      pattern: "prisma\\.operador\\.findUnique"
---

<objective>
Renomear o módulo `vigilantes` da API para `operadores`: mover/renomear a pasta do módulo, atualizar todos os imports TypeScript de `@alerta-vigia/*` para `@opencheck/*`, substituir `prisma.vigilante` por `prisma.operador` em toda a API, e renomear a rota Field API `/vigilante/:id/config` para `/operador/:id/config`.

Purpose: Tornar o código da API TypeScript coerente com os novos nomes de pacotes e modelo Prisma.
Output: Módulo operadores funcional, zero imports @alerta-vigia na API, zero refs prisma.vigilante.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-CONTEXT.md
@apps/api/src/index.ts
@apps/api/src/modules/vigilantes/vigilantes.routes.ts
@apps/api/src/modules/field-api/field-api.middleware.ts
@apps/api/src/modules/field-api/field-api.routes.ts
@apps/api/src/modules/field-api/field-api.service.ts

<interfaces>
<!-- Após plano 01-02, o cliente Prisma expõe: -->
<!-- prisma.operador (não prisma.vigilante) -->
<!-- prisma.operador.findFirst({ where: { id, tenantId } }) -->
<!-- prisma.operador.findUnique({ where: { agentKey } }) -->
<!-- prisma.operador.create({ data: { tenantId, nome, ... } }) -->
<!-- prisma.operador.update({ where: { id }, data: { ... } }) -->

<!-- AgentKeyTipo enum: -->
<!-- 'PONTO' | 'OPERADOR'  (não 'VIGILANTE') -->

<!-- AgentContext type (field-api.middleware.ts): -->
<!-- interface AgentContext { tenantId, pontoId, vigilanteId: string | null, tipo: 'PONTO' | 'OPERADOR' } -->
<!-- NOTA: o campo vigilanteId é mantido como está (é um campo da API externa, não do modelo) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Renomear módulo vigilantes → operadores e atualizar imports @alerta-vigia → @opencheck</name>
  <files>
    apps/api/src/modules/operadores/operadores.routes.ts,
    apps/api/src/index.ts,
    apps/api/src/modules/configuracoes/configuracoes.routes.ts,
    apps/api/src/modules/eventos/eventos.routes.ts,
    apps/api/src/modules/notificacoes/notificacoes.routes.ts,
    apps/api/src/modules/auth/auth.routes.ts,
    apps/api/src/modules/cameras/cameras.routes.ts,
    apps/api/src/modules/ciclos/ciclos.routes.ts,
    apps/api/src/modules/relatorios/relatorios.routes.ts,
    apps/api/src/modules/superadmin/superadmin.routes.ts,
    apps/api/src/modules/webhooks/webhook.routes.ts,
    apps/api/src/modules/assinaturas/assinaturas.routes.ts,
    apps/api/src/modules/assinaturas/webhook.handler.ts,
    apps/api/src/modules/assinaturas/assinatura.service.ts,
    apps/api/src/middleware/assinatura.middleware.ts,
    apps/api/src/jobs/ciclo-alerta.job.ts,
    apps/api/src/jobs/ciclo-agendamento.job.ts,
    apps/api/src/jobs/notificacao.job.ts,
    apps/api/src/jobs/assinatura-sync.job.ts,
    apps/api/src/jobs/index.ts,
    apps/api/src/infra/asaas/asaas.client.ts,
    apps/api/src/infra/ezviz/ezviz.factory.ts
  </files>
  <read_first>
    - apps/api/src/index.ts — ver import e registro de vigilantesRoutes
    - apps/api/src/modules/vigilantes/vigilantes.routes.ts — conteúdo completo para copiar
  </read_first>
  <action>
**Passo 1 — Criar o módulo operadores a partir do vigilantes:**

Criar a pasta `apps/api/src/modules/operadores/` e o arquivo `apps/api/src/modules/operadores/operadores.routes.ts` com o conteúdo de `apps/api/src/modules/vigilantes/vigilantes.routes.ts`, aplicando as seguintes substituições:

No novo arquivo `operadores.routes.ts`:
- `import { prisma } from '@alerta-vigia/database'` → `import { prisma } from '@opencheck/database'`
- Função: `async function gerarCodigoUnico(tenantId: string)`: trocar internamente:
  - `prisma.vigilante.findFirst({ where: { tenantId, codigo } })` → `prisma.operador.findFirst({ where: { tenantId, codigo } })`
  - Mensagem de erro: `'Não foi possível gerar código único para o vigilante'` → `'Não foi possível gerar código único para o operador'`
- Função exportada: `export async function vigilantesRoutes(app: FastifyInstance)` → `export async function operadoresRoutes(app: FastifyInstance)`
- Dentro da função, cada `prisma.vigilante.` → `prisma.operador.`
- Mensagens de texto visíveis ao usuário:
  - `'Vigilante não encontrado'` → `'Operador não encontrado'`
- Comentários de seção: manter ou atualizar como preferir
- `return vigs.map(v => ({ ...v, vigilanteId: v.codigo ?? v.id }))` — MANTER este campo `vigilanteId` no retorno (é campo de API externo compatível com o cliente Windows)

**Passo 2 — Atualizar apps/api/src/index.ts:**
- `import { vigilantesRoutes } from './modules/vigilantes/vigilantes.routes.js'` → `import { operadoresRoutes } from './modules/operadores/operadores.routes.js'`
- `await app.register(vigilantesRoutes, { prefix: '/vigilantes' })` → `await app.register(operadoresRoutes, { prefix: '/operadores' })`
- `import { prisma } from '@alerta-vigia/database'` → `import { prisma } from '@opencheck/database'`

**Passo 3 — Atualizar @alerta-vigia → @opencheck em TODOS os outros arquivos da API:**

Para cada arquivo listado abaixo, substituir TODA ocorrência de `'@alerta-vigia/` por `'@opencheck/` nas linhas de import:

Arquivos a processar (ler cada um, trocar imports, salvar):
- `apps/api/src/modules/configuracoes/configuracoes.routes.ts`
- `apps/api/src/modules/eventos/eventos.routes.ts`
- `apps/api/src/modules/notificacoes/notificacoes.routes.ts`
- `apps/api/src/modules/auth/auth.routes.ts`
- `apps/api/src/modules/cameras/cameras.routes.ts`
- `apps/api/src/modules/ciclos/ciclos.routes.ts`
- `apps/api/src/modules/relatorios/relatorios.routes.ts`
- `apps/api/src/modules/superadmin/superadmin.routes.ts`
- `apps/api/src/modules/webhooks/webhook.routes.ts`
- `apps/api/src/modules/assinaturas/assinaturas.routes.ts`
- `apps/api/src/modules/assinaturas/webhook.handler.ts`
- `apps/api/src/modules/assinaturas/assinatura.service.ts`
- `apps/api/src/middleware/assinatura.middleware.ts`
- `apps/api/src/jobs/ciclo-alerta.job.ts`
- `apps/api/src/jobs/ciclo-agendamento.job.ts`
- `apps/api/src/jobs/notificacao.job.ts`
- `apps/api/src/jobs/assinatura-sync.job.ts`
- `apps/api/src/jobs/index.ts`
- `apps/api/src/infra/asaas/asaas.client.ts`
- `apps/api/src/infra/ezviz/ezviz.factory.ts`

Operação em cada arquivo: encontrar qualquer linha `import ... from '@alerta-vigia/NOME'` e trocar para `import ... from '@opencheck/NOME'`. Não alterar nenhuma outra linha.

**Passo 4 — Manter (não deletar) a pasta vigilantes:**
NÃO deletar `apps/api/src/modules/vigilantes/`. O executor pode opcionalmente deletá-la após confirmar que o typecheck passa, mas não é necessário neste plano.
</action>
  <verify>
    <automated>grep -r "@alerta-vigia" apps/api/src/ && echo "FALHOU: ainda há imports @alerta-vigia na API" || echo "OK: zero imports @alerta-vigia na API"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "@alerta-vigia" apps/api/src/` retorna vazio
    - `cat apps/api/src/index.ts | grep operadoresRoutes` retorna 2 linhas (import + register)
    - `cat apps/api/src/index.ts | grep "/operadores"` retorna 1 linha (prefix do register)
    - `cat apps/api/src/modules/operadores/operadores.routes.ts | grep "operadoresRoutes"` retorna resultado
    - `cat apps/api/src/modules/operadores/operadores.routes.ts | grep "@opencheck/database"` retorna resultado
  </acceptance_criteria>
  <done>Módulo operadores criado, index.ts atualizado para /operadores, todos os imports @alerta-vigia removidos da API</done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar módulo field-api — prisma.vigilante → prisma.operador, tipo OPERADOR, rota renomeada</name>
  <files>
    apps/api/src/modules/field-api/field-api.middleware.ts,
    apps/api/src/modules/field-api/field-api.routes.ts,
    apps/api/src/modules/field-api/field-api.service.ts,
    apps/api/src/modules/field-api/field-api.utils.ts
  </files>
  <read_first>
    - apps/api/src/modules/field-api/field-api.middleware.ts — ver todas as refs prisma.vigilante e 'VIGILANTE'
    - apps/api/src/modules/field-api/field-api.routes.ts — ver rota /vigilante/:id/config
    - apps/api/src/modules/field-api/field-api.service.ts — ver refs prisma.vigilante e resolveVigilanteId
    - apps/api/src/modules/field-api/field-api.utils.ts — verificar se há imports @alerta-vigia
  </read_first>
  <action>
**field-api.middleware.ts — substituições obrigatórias:**

1. `import { prisma } from '@alerta-vigia/database'` → `import { prisma } from '@opencheck/database'`

2. Na interface AgentContext:
   - `tipo: 'PONTO' | 'VIGILANTE'` → `tipo: 'PONTO' | 'OPERADOR'`

3. No bloco "Try as vigilante key":
   - Comentário: `// Try as vigilante key` → `// Try as operador key`
   - `const vigilante = await prisma.vigilante.findUnique({` → `const operador = await prisma.operador.findUnique({`
   - Renomear a variável `vigilante` para `operador` em TODO o bloco seguinte:
     - `if (vigilante) {` → `if (operador) {`
     - `if (!vigilante.ativo)` → `if (!operador.ativo)`
     - Mensagem: `'Vigilante desativado'` → `'Operador desativado'`
     - `if (!vigilante.tenant.ativo)` → `if (!operador.tenant.ativo)`
     - `if (!vigilante.pontos[0])` → `if (!operador.pontos[0])`
     - Mensagem: `'Vigilante não vinculado a nenhum ponto'` → `'Operador não vinculado a nenhum ponto'`
     - `const statusAss = vigilante.tenant.assinatura?.status` → `const statusAss = operador.tenant.assinatura?.status`
     - `await logAcesso('VIGILANTE', vigilante.id, vigilante.tenantId, request)` → `await logAcesso('OPERADOR', operador.id, operador.tenantId, request)`
     - `request.agentCtx = { tenantId: vigilante.tenantId, pontoId: vigilante.pontos[0].id, vigilanteId: vigilante.id, tipo: 'VIGILANTE' }` → `request.agentCtx = { tenantId: operador.tenantId, pontoId: operador.pontos[0].id, vigilanteId: operador.id, tipo: 'OPERADOR' }`
     NOTA: manter o campo `vigilanteId` no agentCtx (é campo de API pública compatível com cliente externo)

4. Na função `logAcesso`:
   - Assinatura: `async function logAcesso(tipo: 'PONTO' | 'VIGILANTE', ...)` → `async function logAcesso(tipo: 'PONTO' | 'OPERADOR', ...)`

5. No agentKeyMiddleware, o prefixo do agentKey:
   - `if (!agentKey?.startsWith('av_'))` — MANTER como está (o prefixo av_ é do token, não do nome do produto)

**field-api.routes.ts — substituições obrigatórias:**

1. `import { prisma } from '@alerta-vigia/database'` → `import { prisma } from '@opencheck/database'`

2. Rota renomeada:
   - `app.get('/vigilante/:vigilanteId/config', async (request, reply) => {` → `app.get('/operador/:operadorId/config', async (request, reply) => {`
   - `const { vigilanteId } = request.params as { vigilanteId: string }` → `const { operadorId } = request.params as { operadorId: string }`
   - `const vigilante = await prisma.vigilante.findFirst({ where: { id: vigilanteId, tenantId, ativo: true }, ...` → `const operador = await prisma.operador.findFirst({ where: { id: operadorId, tenantId, ativo: true }, ...`
   - `if (!vigilante)` → `if (!operador)`
   - Código de erro: `{ erro: 'VIGILANTE_NAO_ENCONTRADO', mensagem: 'Vigilante não encontrado neste tenant' }` → `{ erro: 'OPERADOR_NAO_ENCONTRADO', mensagem: 'Operador não encontrado neste tenant' }`
   - `if (!vigilante.pontos[0])` → `if (!operador.pontos[0])`
   - `const ctx = { tenantId, pontoId: vigilante.pontos[0].id, vigilanteId, tipo: 'VIGILANTE' as const }` → `const ctx = { tenantId, pontoId: operador.pontos[0].id, vigilanteId: operadorId, tipo: 'OPERADOR' as const }`
   - `const config = await getConfig(ctx)` — manter
   - `agentKeyPonto: vigilante.pontos[0].agentKey` → `agentKeyPonto: operador.pontos[0].agentKey`

3. No endpoint POST /checkin (body):
   - `const body = (request.body ?? {}) as { vigilanteId?: string; observacao?: string }` — MANTER (campo público da API)

4. No endpoint POST /panico:
   - `vigilanteId?: string` no body — MANTER (campo público da API)

**field-api.service.ts — substituições obrigatórias:**

1. `import { prisma } from '@alerta-vigia/database'` → `import { prisma } from '@opencheck/database'`

2. Função `resolveVigilanteId`:
   - `const vig = await prisma.vigilante.findFirst({ where: { tenantId, codigo: valor, ativo: true } })` → `const op = await prisma.operador.findFirst({ where: { tenantId, codigo: valor, ativo: true } })`
   - `return vig?.id ?? null` → `return op?.id ?? null`

3. Função `getConfig` — dentro do include de `prisma.ponto.findUnique`:
   - `vigilantes: { where: { ativo: true }, select: { id: true, nome: true, telefone: true, codigo: true } }` → `operadores: { where: { ativo: true }, select: { id: true, nome: true, telefone: true, codigo: true } }`
   - `const vigilante = ctx.vigilanteId ? ponto.vigilantes.find(...) ?? ponto.vigilantes[0] ?? null : ponto.vigilantes[0] ?? null` → `const operador = ctx.vigilanteId ? ponto.operadores.find(...) ?? ponto.operadores[0] ?? null : ponto.operadores[0] ?? null`
   - `const vigilanteId = vigilante?.codigo ?? vigilante?.id ?? null` → `const vigilanteId = operador?.codigo ?? operador?.id ?? null`
   - No retorno: `vigilanteId` — manter o nome do campo (API pública)
   - `vigilantes: ponto.vigilantes.map(v => ({...}))` → `vigilantes: ponto.operadores.map(v => ({...}))` (manter chave `vigilantes` no retorno por compatibilidade com cliente externo, mas ler de `ponto.operadores`)

**field-api.utils.ts:**
1. Se houver `import ... from '@alerta-vigia/...'` → trocar para `@opencheck/...`
2. Nenhuma outra mudança necessária neste arquivo.
</action>
  <verify>
    <automated>grep -r "prisma\.vigilante\|@alerta-vigia\|'VIGILANTE'\|\"VIGILANTE\"" apps/api/src/modules/field-api/ && echo "FALHOU: ainda há refs Vigilante no field-api" || echo "OK: field-api sem Vigilante"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "prisma.vigilante" apps/api/src/modules/field-api/field-api.middleware.ts` retorna vazio
    - `grep "prisma.operador" apps/api/src/modules/field-api/field-api.middleware.ts` retorna resultado
    - `grep "tipo: 'OPERADOR'" apps/api/src/modules/field-api/field-api.middleware.ts` retorna resultado
    - `grep "/operador/:operadorId/config" apps/api/src/modules/field-api/field-api.routes.ts` retorna resultado
    - `grep "prisma.operador" apps/api/src/modules/field-api/field-api.service.ts` retorna resultado
    - `grep "@alerta-vigia" apps/api/src/modules/field-api/` retorna vazio
    - O campo `vigilanteId` nos bodies de request e no agentCtx é mantido (não renomeado)
  </acceptance_criteria>
  <done>field-api completamente atualizado: prisma.operador, tipo OPERADOR, rota /operador/:id/config, zero refs @alerta-vigia</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| cliente Field API → endpoint /operador/:id/config | Autenticação via agentKey header |
| body.vigilanteId → resolveVigilanteId | Input não-confiável; já validado pelo middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | agentKeyMiddleware | mitigate | Prefixo av_ mantido + lookup no banco; renomeação não altera a lógica de autenticação |
| T-01-03-02 | Information Disclosure | erro OPERADOR_NAO_ENCONTRADO | accept | Mensagem de erro não revela dados internos, apenas indica não-encontrado |
</threat_model>

<verification>
1. `grep -r "@alerta-vigia" apps/api/src/` → vazio
2. `grep -r "prisma\.vigilante" apps/api/src/` → vazio
3. `grep -r "prefix.*operadores" apps/api/src/index.ts` → 1 resultado
4. `grep "/operador/:operadorId" apps/api/src/modules/field-api/field-api.routes.ts` → 1 resultado
</verification>

<success_criteria>
- Zero imports `@alerta-vigia` em todo `apps/api/src/`
- Zero ocorrências de `prisma.vigilante` em toda a API
- Rota registrada como `/operadores` (não `/vigilantes`) no index.ts
- Rota Field API `/operador/:operadorId/config` existe (não `/vigilante/:vigilanteId/config`)
- Campo `vigilanteId` mantido nos bodies de API para compatibilidade com cliente externo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-rebranding-alerta-vigia-opencheck-vigilante-operador/01-03-SUMMARY.md`
</output>
