name: Build & Deploy

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  OWNER: arnaldomb

jobs:
  build:
    name: Build & Push Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.OWNER }}/opencheck-api:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push Web
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.OWNER }}/opencheck-web:latest
          build-args: |
            NEXT_PUBLIC_API_URL=https://api.opencheck.ggtronic.com.br
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push Migrate
        uses: docker/build-push-action@v5
        with:
          context: .
          file: packages/database/Dockerfile.migrate
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.OWNER }}/opencheck-migrate:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: SSH & deploy
        uses: appleboy/ssh-action@v1
        with:
          host: 193.203.182.2
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /docker/opencheck
            git pull origin main
            docker compose pull
            docker compose up -d --remove-orphans
            docker image prune -f
